export type ColourConfigType = {
  dialogueNodeTree: {
    nonActiveOpacity: number;
  };
  coreNode: {
    primary: string;
    secondary: string;
    highlight: string;
  };
  rootNode: {
    primary: string;
    secondary: string;
    highlight: string;
  };
  promptNode: {
    primary: string;
    secondary: string;
    highlight: string;
  };
  responseNode: {
    primary: string;
    secondary: string;
    highlight: string;
  };
  linkNode: {
    primary: string;
    secondary: string;
    highlight: string;
  };
};
